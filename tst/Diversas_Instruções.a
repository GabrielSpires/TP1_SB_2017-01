	loadc A0 10							; load 10 -> A0
	loadc A1 5							; load 5 -> A1
	add A0 A1							; A0 = A0 + A1
	storei A0 IO						; Imprime o valor de A0 na tela
	subtract A0 A1						; A0 = A0 - A1
	storei A0 IO						; Imprime o valor de A0 na tela
	move A2 A0						; A2 = A0
	loadc A3 -3							; A3 = -3
	multiply A2 A3						; A2 = A2 * A3
	storei A2 IO						; Imprime na tela o valor de A2
	loadi A4 _input						; A4 = reg
	clear A0							; A0 = 0
	add A0 A4							; A0 = A0 + A4
	exit                   
_input: .data 2 3						; A5 = valor digitado pelo usuário